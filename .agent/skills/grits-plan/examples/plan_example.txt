User: "I need to add a persistence layer for the new cache."

Agent (Planner):
1. `gr pulse` (Checks focus: gr-123 "Cache implementation")
2. `gr star --format symbols` (Researching cache logic)
   Output: 
   src/cache/mod.rs::CacheManager
   src/cache/store.rs::MemoryStore
3. `gr context assemble` (Analyzing files)
4. `gr update --design "1. Create SqliteStore in src/cache/sqlite.rs\n2. Implement Store trait for SqliteStore\n3. Update CacheManager to use SqliteStore based on config"`
5. `gr update --acceptance-criteria "1. SqliteStore implements all Store methods\n2. Cache persists after restart\n3. Unit tests pass for SqliteProvider"`
6. `gr update --scan-file "src/cache/sqlite.rs"` (Attaching files for Coder)
